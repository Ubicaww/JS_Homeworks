import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#00a046',
      contrastText: '#fff',
    },
    secondary: {
      main: '#f5f5f5',
    },
    background: {
      default: '#e9e9e9',
    },
  },
});